{
  "name": "app",
  "private": true,
  "version": "0.0.5",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm version patch && vite build",
    "preview": "vite preview",
    "deploy": "npm version patch && cross-env VITE_GROQ_API_KEY= vite build && npx gh-pages -d dist",
    "check": "svelte-check --tsconfig ./tsconfig.app.json && tsc -p tsconfig.node.json"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.0.0",
    "@tsconfig/svelte": "^5.0.8",
    "@types/node": "^24.12.2",
    "cross-env": "^10.1.0",
    "svelte": "^5.55.4",
    "svelte-check": "^4.4.6",
    "typescript": "~6.0.2",
    "vite": "^8.0.10"
  },
  "dependencies": {
    "@huggingface/transformers": "^4.2.0",
    "lucide-svelte": "^1.0.1",
    "perfect-freehand": "^1.2.3"
  }
}
